<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_root"
    android:layout_width="@dimen/inputs_item_container_width"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:nextFocusUp="@id/ll_root"
    android:nextFocusDown="@id/ll_root"
    android:orientation="vertical"
    tools:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="@dimen/inputs_item_icon_size"
        android:layout_height="@dimen/inputs_item_icon_size"
        tools:src="@drawable/ic_unfocused_t2" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#737474"
            android:textSize="@dimen/inputs_item_title_text_size"
            tools:text="Аналоговое" />

        <ImageView
            android:id="@+id/iv_indicator"
            android:layout_width="@dimen/inputs_item_indicator_size"
            android:layout_height="@dimen/inputs_item_indicator_size"
            android:layout_marginStart="@dimen/inputs_item_indicator_margin_start"
            android:layout_gravity="center_vertical"
            android:layout_marginTop="@dimen/inputs_item_indicator_margin_top"
            android:src="@drawable/ic_oval"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

</LinearLayout>